---
description: 项目规则，包含行为、说中文、禁止项与日志规范
alwaysApply: true
---

# 行为准则

## 常规
- 只修改指定部分
- 没有明确要求时，不要主动修改、接入、联动、重构任何用户未点名的代码；若你判断还需要额外改动，先询问我
- **重要**：每次修改代码之前，一定要以最新的代码为准
- Plan模式下，反复思考，只要有疑问就询问我，直到没有任何疑问

## 说中文
- **十分重要**：任何输出，包括并不限于plan/checklist/spec/tasks 等 markdown 或文本，必须中文。
- **注释**：代码中注释必须使用中文，日志英文

## 注释规则
- 函数级注释
    公开函数：添加标准doc注释（功能描述、入参、出参、异常，返回值），没有的项省略
    私有函数：双斜杠简要一句话注释
    成员变量：公开：/**...*/简要一句话注释；私有：双斜杠简要一句话注释

- 函数内代码注释
    - 简单顺序执行逻辑：不添加注释
    - 仅为以下关键逻辑注释：多层条件嵌套、循环内业务计算 / 判断、非直观算法、核心业务 / 边界值处理
    - 一段完整相关代码仅注释 1 次，注释写在逻辑块顶部，极简描述业务意图，不逐行冗余

## 禁止项
- 严格按需求输出，不加额外内容
- 不重构未要求代码
- 不输出lint检查结果
- 不说开场白/结束语
- 不生成测试代码和示例代码
- 不重复用户话
- 不输出代码概览
- **不要**主动运行 Gradle 等编译/构建命令
- 不使用 `coerceAtLeast`、`coerceAtMost`、`coerceIn`、`minOf`、`maxOf`，统一使用 `min` / `max`

## 任务完成后的输出格式
- 收尾不写实现细节与废话
- 按**本轮改动源文件数**二选一：
    - **≤2** 仅一句中文
    - **≥3** 用 `-` 列表（类名勿全路径，每行一句）且末行「总结：」一句整体

# kotlin/kts文件日志规范

- 优先 `logdNoFile`（仅控制台）替代Log.d
- 函数名拆分（如 `onDeviceListChanged` → on deviceList changed）
- 严重错误使用 `loge { "" + e.message }` / `logEx(throwable = e) { "xxx" }`
