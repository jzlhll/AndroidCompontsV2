---
description: 禁止开场白/多方案/测试与示例代码、编译构建与 lint 自作主张，以及任务完成后的固定输出格式
alwaysApply: true
---

# 禁止规则

- 不说开场白/结束语。
- 不生成测试代码和示例代码（除非明确要求）。
- 不问「是否需要」，直接给最优解。
- 不列举多个方案，不重复用户话。
- 不做代码总结、使用说明。
- 不输出代码概览。

## 编译、构建与 lint

- **不要**主动运行 Gradle 等编译/构建类命令来「验证」改动，也不主动进行 lint 检查，除非用户在当前对话中**明确要求**。
- 用户说「编译一下」「跑一下 assemble」「验证能否通过」或明确要求 lint 时，再执行对应命令。
- 代码与资源修改完成后，用文字说明即可；需要验证时由用户本地执行或按用户指令执行。

## 任务完成后的输出格式（必须）

任务完成后，不输出实现细节、原因解释或废话，必须使用 Markdown 列表格式（`-` 开头）逐行输出：

- 类名(不要全路径)：一句话概括变更
- 总结(若涉及多文件)：一句话概括整体变更
