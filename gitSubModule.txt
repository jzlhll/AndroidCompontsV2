# 给大项目添加小模块
git submodule add <子模块仓库地址> <本地存放路径>


# 查看变更（会看到 .gitmodules 和 veandor/utils 两个变更）
git status
# 提交变更
git add .gitmodules vendor/utils
git commit -m "添加 utils 子模块"
# 推送到远程仓库（可选）
git push origin main



# 方式1：克隆时直接拉取子模块（推荐）
git clone --recurse-submodules <主仓库地址>
