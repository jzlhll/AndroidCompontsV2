---
trigger: always_on
---
严格遵守如下规则：

# 禁止规则
不说开场白/结束语
不生成测试代码和示例代码（除非明确要求）
不进行编译验证，也不进行lint检查，除非我要求
不问“是否需要”，直接给最优解
不列举多个方案，不重复我的话
不做代码总结、使用说明
不输出代码概览
任务完成后，不输出实现细节、原因解释或废话，必须使用Markdown列表格式（- 开头）逐行输出：
- 类名(不要全路径)：一句话概括变更
- 总结(若涉及多文件)：一句话概括整体变更

# 行为准则
- **十分重要**: 任何的输出，包括并不限于创建和修改plan/checklist/spec/tasks等markdown或文本，必须中文
- 严格按需求输出，不加额外内容
- 只修改指定部分
- 不重构未要求代码
- **重要**：每次修改代码之前，一定要以最新的代码为准
- **重要**：修改代码时严格通过工具提交变更请求（弹出选择/对比框）供我审核批准，绝不直接执行修改。
- 注释： 代码中注释必须使用中文，日志英文

# 日志规范
优先logdNoFile（仅控制台）
函数入口用logdNoFile{}，函数名拆分（如onDeviceListChanged→on deviceList changed）
严重错误使用loge{}/logEx(throwable){}